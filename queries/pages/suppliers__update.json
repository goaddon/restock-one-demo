{
  "update_supplier": {
    "klass": "suppliers",
    "query": {
      "company_id": "{{ company_id }}",
      "_id": "{{ _id }}"
    },
    "document": {
      "$set": {
        "name": "{{ name }}",
        "url": "{{ url }}",
        "email": "{{ email }}"
      }
    }
  }
}